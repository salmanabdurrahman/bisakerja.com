{
  "name": "bisakerja",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.23.0",
  "scripts": {
    "lint": "pnpm --filter web lint && make -C apps/api lint",
    "typecheck": "pnpm --filter web typecheck && make -C apps/api vet",
    "test": "pnpm --filter web test && make -C apps/api test",
    "build": "pnpm --filter web build && make -C apps/api build"
  }
}
